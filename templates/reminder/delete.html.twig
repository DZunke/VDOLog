{% extends 'base.html.twig' %}

{% block content %}
    <div class="page-title">
        <h1>Erinnerung "{{ reminder.title }}" löschen</h1>
    </div>

    <div class="row row-cards">
        <div class="col-12">
            <form method="post"
                  action="{{ path('reminder_delete', {'id': reminder.id}) }}"
                  class="card">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reminder.id) }}">
                <div class="card-body">
                    <button class="btn btn-danger">Löschen</button>
                    <a href="{{ path('dashboard') }}" class="btn btn-secondary">Abbrechen</a>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
